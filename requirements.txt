twikit==2.3.3
curl_cffi>=0.7.0
# Playwright HANYA untuk grab cookies via browser (opsional, desktop Linux/macOS).
# Di Termux gak dipakai (Chromium x86 gak jalan di ARM). Install manual kalau perlu:
#   pip install playwright && playwright install chromium
